name: 005 - AWS Deploy - ECS
on:
  workflow_dispatch:
jobs:
  deploy:
    uses: ./.github/workflows/aws-terraform.yml
    with:
      action: apply
      targets: module.vpc module.bedrock module.rds module.ecs module.svc_agents module.svc_datalake module.svc_gateway
      label: ECS
    secrets: inherit
