name: 006 - AWS Destroy - Cognito
on:
  workflow_dispatch:
jobs:
  destroy:
    uses: ./.github/workflows/aws-terraform.yml
    with:
      action: destroy
      targets: module.cognito
      label: Cognito
    secrets: inherit
