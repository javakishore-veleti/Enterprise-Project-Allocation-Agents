name: 001 - AWS Destroy - VPC
on:
  workflow_dispatch:
jobs:
  destroy:
    uses: ./.github/workflows/aws-terraform.yml
    with:
      action: destroy
      targets: module.vpc
      label: VPC
    secrets: inherit
