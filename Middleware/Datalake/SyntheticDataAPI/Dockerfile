FROM python:3.12-slim

WORKDIR /app
ENV PYTHONUNBUFFERED=1 PIP_NO_CACHE_DIR=1

COPY pyproject.toml ./
COPY src ./src
RUN pip install .

EXPOSE 8000
CMD ["uvicorn", "epaa_datalake.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
