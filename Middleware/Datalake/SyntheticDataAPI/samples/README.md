# Sample fixtures

Small curated examples of the shape produced by the generator (the live data is
generated into Postgres, not committed). Generate the full set with:

```bash
EMBEDDING_PROVIDER=hash epaa-datalake generate --migrate-first --employees 60 --projects 20
```
